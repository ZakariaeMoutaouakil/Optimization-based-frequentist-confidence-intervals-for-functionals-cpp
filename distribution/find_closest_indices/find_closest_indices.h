#ifndef FIND_CLOSEST_INDICES_H
#define FIND_CLOSEST_INDICES_H

#include <vector>

std::vector<int> find_closest_indices(const std::vector<std::vector<float>>& vectors, const std::vector<float>& values);

#endif // FIND_CLOSEST_INDICES_H
